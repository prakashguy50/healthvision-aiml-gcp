spring:
  application:
    name: llm-service
  cloud:
    gcp:
      project-id: ${GCP_PROJECT_ID}
      vertex:
        ai:
          region: ${GCP_REGION}

vertex:
  ai:
    model: ${VERTEX_AI_MODEL:gemini-pro}

server:
  port: 8080